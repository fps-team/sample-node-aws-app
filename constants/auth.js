const DEFAULT_CREDENTIALS_EMAIL = "jemimah.beryl.sai@gmail.com";
const DEFAULT_CREDENTIALS_PASSWORD = "G64*FuB93";
const DEFAULT_SITE_CONTENT_URI = "samplesite93dev220834";

const TABLEAU_LOGIN_CREDENTIALS = {
  "credentials": {
    "name": DEFAULT_CREDENTIALS_EMAIL,
    "password": DEFAULT_CREDENTIALS_PASSWORD,
    "site": {
      "contentUrl": DEFAULT_SITE_CONTENT_URI
    }
  }
};

module.exports = { TABLEAU_LOGIN_CREDENTIALS }